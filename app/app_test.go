package app_test

import (
	"testing"
)

func TestApp(t *testing.T) {
	// bootstrap := app.App{AppName: "Test Go Rinha"}

	// a := bootstrap.Setup()

	// requestBody := []byte(`{"apelido": "apelido"}`)
	// body := bytes.NewReader(requestBody)

	// req := httptest.NewRequest("POST", "http://localhost:3000/pessoas", body)
	// req.Header.Set("accept", "application/json")

	// resp, _ := a.Test(req)

	// if resp.StatusCode != 422 {
	// 	t.Errorf("Expected status 422 but received %d", resp.StatusCode)
	// }

	// fmt.Printf("%v", resp.Body)

}
